<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="person">💲</span>
  </p>
  <p>
    <button class="btn btn-primary">Subscribe!</button>
  </p>
</div>
