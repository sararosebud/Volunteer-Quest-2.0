const router = require('express').Router();
const { Event, User } = require('../models');
const withAuth = require('../utils/auth');
// all req that get sent from frontend to backend for profile page 


module.exports = router;