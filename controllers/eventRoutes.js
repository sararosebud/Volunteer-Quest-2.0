const router = require('express').Router();
const { Event, User } = require('../models');
// const withAuth = require('../utils/auth'); // might not need this since all events will show
// all req sent from frontend for event page go here


module.exports = router;